generator client {
  provider        = "prisma-client-js"
  runtime         = "nodejs"
  moduleFormat    = "esm"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model tb_etapa {
  id_etapa          Int                 @id @default(autoincrement())
  id_obra           Int?
  nome              String?
  previsao_inicio   DateTime?           
  previsao_fim      DateTime?           
  id_status         Int?
  tb_status         tb_status?          @relation(fields: [id_status], references: [id_status], onDelete: NoAction, onUpdate: NoAction)
  tb_obra           tb_obra?            @relation(fields: [id_obra], references: [id_obra], onDelete: NoAction, onUpdate: NoAction)
  tb_etapa_material tb_etapa_material[]

  @@index([id_obra])
}

model tb_etapa_material {
  id_etapa    Int
  id_material Int
  quantidade  Decimal?    
  tb_etapa    tb_etapa    @relation(fields: [id_etapa], references: [id_etapa], onDelete: Cascade, onUpdate: NoAction)
  tb_material tb_material @relation(fields: [id_material], references: [id_material], onDelete: NoAction, onUpdate: NoAction)

  @@id([id_etapa, id_material])
}

model tb_fabricante {
  id_fabricante          Int                      @id @default(autoincrement())
  nome_fabricante        String
  tb_material_fabricante tb_material_fabricante[]
}

model tb_material {
  id_material             Int                       @id @default(autoincrement())
  nome                    String
  unidade_medida          String?
  preco_unitario          Decimal?                  
  criado_em               DateTime?                 @default(now()) 
  densidade               Decimal?                  
  resistencia             Decimal?                  
  volume_unitario         Decimal?                  
  peso_unitario           Decimal?                  
  quantidade_atual        Decimal?                  
  observacoes             String?
  tb_etapa_material       tb_etapa_material[]
  tb_material_fabricante  tb_material_fabricante[]
  tb_material_requisitado tb_material_requisitado[]
}

model tb_material_fabricante {
  id_material   Int
  id_fabricante Int
  tb_fabricante tb_fabricante @relation(fields: [id_fabricante], references: [id_fabricante], onDelete: NoAction, onUpdate: NoAction)
  tb_material   tb_material   @relation(fields: [id_material], references: [id_material], onDelete: NoAction, onUpdate: NoAction)

  @@id([id_material, id_fabricante])
}

model tb_material_requisitado {
  id_requisicao Int
  id_material   Int
  quantidade    Decimal?      
  tb_material   tb_material   @relation(fields: [id_material], references: [id_material], onDelete: NoAction, onUpdate: NoAction)
  tb_requisicao tb_requisicao @relation(fields: [id_requisicao], references: [id_requisicao], onDelete: NoAction, onUpdate: NoAction)

  @@id([id_requisicao, id_material])
}

model tb_obra {
  id_obra                Int               @id @default(autoincrement())
  nome                   String
  tipo_obra              String?            // Residencial, Comercial, Industrial
  logradouro             String?
  numero                 String?           
  bairro                 String?
  cidade                 String?
  estado                 String?           
  cep                    String?           
  latitude               Decimal?          
  longitude              Decimal?          
  data_inicio            DateTime?         
  previsao_termino       DateTime?         
  data_termino_real      DateTime?         
  valor_orcado           Decimal?          
  custo_atual            Decimal?          
  observacoes            String?           
  id_usuario_responsavel Int?
  id_status              Int?

  tb_etapa               tb_etapa[]
  tb_status              tb_status?        @relation(fields: [id_status], references: [id_status], onDelete: NoAction, onUpdate: NoAction)
  tb_usuario             tb_usuario?       @relation(fields: [id_usuario_responsavel], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  tb_requisicao          tb_requisicao[]
  tb_usuario_obra        tb_usuario_obra[]
  tb_documento           tb_documento[]
  tb_obra_cliente        tb_obra_cliente[]

  @@index([id_usuario_responsavel])
}

model tb_papel {
  id_papel        Int               @id @default(autoincrement())
  nome            String
  tb_usuario_obra tb_usuario_obra[]
}

model tb_requisicao {
  id_requisicao           Int                       @id @default(autoincrement())
  id_obra                 Int?
  quantidade_planejada    Decimal?                  
  quantidade_usada        Decimal?                  
  data_registro           DateTime?                 @default(now()) 
  id_usuario              Int?
  tb_material_requisitado tb_material_requisitado[]
  tb_usuario              tb_usuario?               @relation(fields: [id_usuario], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  tb_obra                 tb_obra?                  @relation(fields: [id_obra], references: [id_obra], onDelete: NoAction, onUpdate: NoAction)

  @@index([id_obra])
}

model tb_status {
  id_status Int        @id @default(autoincrement())
  nome      String
  tb_etapa  tb_etapa[]
  tb_obra   tb_obra[]
}

model tb_usuario {
  id_usuario                   Int               @id @default(autoincrement())
  nome                         String
  funcao                       String?
  telefone                     String?
  email                        String?
  username                     String?
  senha                        String?
  tipo_registro_profissional   String?
  numero_registro_profissional String?
  tipo_usuario                 String?
  tb_obra                      tb_obra[]
  tb_requisicao                tb_requisicao[]
  tb_usuario_obra              tb_usuario_obra[]
}

model tb_usuario_obra {
  id_usuario Int
  id_obra    Int
  id_papel   Int?
  tb_papel   tb_papel?  @relation(fields: [id_papel], references: [id_papel], onDelete: NoAction, onUpdate: NoAction)
  tb_obra    tb_obra    @relation(fields: [id_obra], references: [id_obra], onDelete: NoAction, onUpdate: NoAction)
  tb_usuario tb_usuario @relation(fields: [id_usuario], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)

  @@id([id_usuario, id_obra])
}

model tb_cliente {
  id_cliente   Int               @id @default(autoincrement())
  nome_razao   String
  cpf_cnpj     String            @unique 
  telefone     String?           
  email        String?
  criado_em    DateTime?         @default(now()) 
  tb_obra_cliente tb_obra_cliente[]
}

model tb_obra_cliente {
  id_obra    Int
  id_cliente Int
  tb_obra    tb_obra    @relation(fields: [id_obra], references: [id_obra], onDelete: Cascade, onUpdate: NoAction)
  tb_cliente tb_cliente @relation(fields: [id_cliente], references: [id_cliente], onDelete: Cascade, onUpdate: NoAction)

  @@id([id_obra, id_cliente])
}

model tb_documento {
  id_documento Int       @id @default(autoincrement())
  id_obra      Int
  nome         String
  tipo         String?    // Planta, Contrato, Licença, Foto, etc
  url          String
  data_upload  DateTime? @default(now()) 
  tb_obra      tb_obra   @relation(fields: [id_obra], references: [id_obra], onDelete: Cascade, onUpdate: NoAction)
}
